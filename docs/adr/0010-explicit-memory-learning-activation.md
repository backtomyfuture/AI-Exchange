---
status: accepted
---

# Activate Memory Learning explicitly

Memory Learning remains valuable but is disabled by default in a Greenfield Deployment. Reading existing Preference Memory, Style Profile, and processing experience may continue during email processing; any LLM analysis or persistent write that derives new memory requires an explicit operator-enabled activation so a normal Inbound Email never silently creates learning cost or state.
